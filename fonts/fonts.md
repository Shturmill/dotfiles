# For correct displaying all fonts  

For correct displaying of all symbols you should install all nerd fonts


```
sudo pacman -S $(pacman -Sgq nerd-fonts)
```

and clear your cache 

```
fc-cache -fv
```

then reboot your system